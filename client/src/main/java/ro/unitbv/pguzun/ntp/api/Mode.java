package ro.unitbv.pguzun.ntp.api;

public enum Mode {
    MODE_RESERVED,
    MODE_SYMMETRIC_ACTIVE,
    MODE_SYMMETRIC_PASSIVE,
    MODE_CLIENT ,
    MODE_SERVER,
    MODE_BROADCAST ,
    MODE_CONTROL_MESSAGE,
    MODE_PRIVATE;
    
    public static void main(String[] args) {
    }
}
